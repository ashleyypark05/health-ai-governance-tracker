# Copy these keys into your Streamlit Community Cloud app's
# Settings → Secrets (do not commit a real secrets.toml).
#
# Top-level keys here are also exposed as os.environ vars on
# Streamlit Community Cloud, which is how pipeline/app/main.py
# and pipeline/digest.py read them.

OPENAI_API_KEY = "sk-..."
BEEHIIV_API_KEY = ""
BEEHIIV_PUB_ID = ""
